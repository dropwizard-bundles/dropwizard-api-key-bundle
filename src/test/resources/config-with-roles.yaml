authentication:
  cache-spec: "maximumSize=1000, expireAfterAccess=10m"

  basic-http:
    realm: Authentication
    keys:
      client: secret

authorization:
  roles:
    client:
      - admin
      - user